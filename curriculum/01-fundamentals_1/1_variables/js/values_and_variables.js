
let js = "Amazing";  // Declare a string variable 'js' and assign it the value "Amazing"
console.log(40 + 8 + 23 - 10);  // Output the result of the arithmetic expression
console.log("Saeed");  // Output the string "Saeed"
console.log(23);  // Output the number 23

let firstName = "Mohamed";  // Declare a string variable 'firstName' and assign it the value "Mohamed"
console.log(firstName);  // Output the value of 'firstName'

let myFirstJob = "Programmer";  // Declare a string variable 'myFirstJob' and assign it the value "Programmer"
let my_current_job = "Teacher";  // Declare a string variable 'my_current_job' and assign it the value "Teacher"

// [X] let jop1 = "Programmer";  // Incorrect variable name (typo), should be 'myFirstJob'
// [X] let jop2 = "Teacher";  // Incorrect variable name (typo), should be  'myCurrentJob'

console.log(myFirstJob);  // Output the value of 'myFirstJob'

let $function = 27;  // Declare a variable '$function' and assign it the value 27 (valid name with a dollar sign)
let lastName = "Ali";  // Declare a string variable 'lastName' and assign it the value "Ali"

// [X] let name = "Saeed";  // Avoid using 'name' as it's a common built-in property of the window object
let person = "Saeed";  // Declare a string variable 'person' and assign it the value "Saeed"

let PI = 3.1415;  // Declare a variable 'PI' and assign it the value of Pi (constant approximation)

let year = 1991;  // Declare a variable 'year' and assign it the value 1991 (likely birth year)
